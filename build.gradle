apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "Hangman"
version = '0.0'
mainClassName = "App"

repositories {
 mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
